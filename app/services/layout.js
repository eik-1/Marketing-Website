export const metadata = {
  title: "Services | Oddstone",
  description:
    "We don't sell packages. We build plans around your goals, budget, and what makes sense for your growth.",
};

export default function ServicesLayout({ children }) {
  return children;
}
